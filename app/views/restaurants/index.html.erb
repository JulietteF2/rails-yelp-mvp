<div class="container">
  <h1>Browse Restaurants</h1>
  <hr>
  <br>

  <ul>
    <% @restaurants.each do |r| %>
      <li><%= link_to r.name, restaurant_path(r) %>, <%= r.address %><!-- - <strong><%= r.reviews %> stars</strong>--></li>
      <br>
    <% end %>
  </ul>
</div>
